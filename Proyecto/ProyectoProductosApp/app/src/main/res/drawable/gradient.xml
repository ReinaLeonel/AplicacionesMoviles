<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Tipo de degradado (linear, radial, sweep) -->
    <!-- Ángulo del degradado (0 - 360 grados) -->
    <!-- Color inicial del degradado -->
    <!-- Color final del degradado -->
    <!-- Color intermedio opcional -->
    <!-- Posición x del color intermedio (0 - 1) -->
    <!-- Posición y del color intermedio (0 - 1) -->
    <!--<gradient
        android:type="linear"
        android:angle="0"
        android:startColor="#FF0000"
        android:endColor="#0000FF"
        android:centerColor="#00FF00"
        android:centerX="0.5"
        android:centerY="0.5"
    /> -->
    <gradient
        android:type="linear"
        android:angle="0"
        android:startColor="#9C3FE4"
        android:endColor="#C65647"
        android:centerX="0.5"
        android:centerY="0.5"
        />
    <corners android:radius="10dp" />
</shape>